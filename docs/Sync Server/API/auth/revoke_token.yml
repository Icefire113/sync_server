info:
  name: revoke_token
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}/api/auth/revoke_token"
  body:
    type: json
    data: "{\r\n  \"id\": 35\r\n}"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Destroys a personal access token by it's id, revoking it
