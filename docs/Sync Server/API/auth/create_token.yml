info:
  name: create_token
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/api/auth/create_token"
  body:
    type: json
    data: "{\r

      \  \"name\": \"\",\r

      \  \"duration_days\": 100\r

      }"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Creates a new access token with a given duration in days
