info:
  name: token_info
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/api/auth/token_info?token=cfs_something"
  params:
    - name: token
      value: cfs_something
      type: query
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{TEST_AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Gets the information surrounding a token, must be authenticated as the same user as the token
