info:
  name: version
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/version"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Just responds with the currently running sync_server version, also functions as a healthcheck to poke and see if the API is running
